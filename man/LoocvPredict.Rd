% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LoocvPredict.R
\name{LoocvPredict}
\alias{LoocvPredict}
\title{Perform leave-one-out cross-validation (LOOCV) for SE prediction}
\usage{
LoocvPredict(
  scdata,
  scmeta,
  Sample = "Sample",
  CellType = "CellType",
  SE = "SE",
  repeats = 1,
  ncores = 4,
  scale = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{scdata}{A gene expression matrix (genes x cells).}

\item{scmeta}{A data.frame containing metadata for each cell.
Row names of `scmeta` should match the column names in `scdata`.}

\item{Sample}{Character. Column name in `scmeta` specifying sample IDs.
If fewer than two unique samples are present, cells are randomly split
into training and test sets within each cell type.}

\item{CellType}{Character. Column name specifying cell type annotations in `scmeta`.}

\item{SE}{Character. Column name specifying spatial ecotype labels in `scmeta`.}

\item{repeats}{Integer. Number of cross-validation repeats.}

\item{ncores}{Integer. Number of cores for parallel computation.}

\item{scale}{Boolean specifying whether to perform univariance normalization
for training and validation data (default: TRUE).}

\item{verbose}{Boolean specifying whether to print the log messages.}

\item{...}{Additional arguments passed to \code{NMFGenerateWList}.}
}
\value{
The input `scmeta` data.frame with an added column `cvPred`
containing predicted SE labels for each cell.
}
\description{
Trains NMF-based spatial ecotype (SE) recovery models using subsets of
single-cell data and evaluates predictions on held-out data. Supports
repeated cross-validation and parallel execution.
}
\details{
For each repeat:
\itemize{
  \item If multiple samples are available, performs leave-one-sample-out CV
  \item Otherwise, performs random stratified splitting within cell types
  \item Trains NMF models using \code{NMFGenerateWList}
  \item Predicts SE labels using \code{RecoverSE}
}
Predictions across repeats are aggregated by majority vote per cell.
}
