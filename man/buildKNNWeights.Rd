% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ComputeSEAbundanceBySN.R
\name{buildKNNWeights}
\alias{buildKNNWeights}
\title{Neighbor-weight construction and aggregation}
\usage{
buildKNNWeights(
  ref_coords,
  query_coords = ref_coords,
  k = 10,
  radius = Inf,
  include.self = TRUE
)
}
\arguments{
\item{ref_coords}{data.frame/matrix of coordinates for the *source* units
(rows = e.g. cells or spots).}

\item{query_coords}{data.frame/matrix of coordinates for the *output*
units (rows = e.g. spatial neighborhoods or spots), columns X, Y. Defaults to `ref_coords`.}

\item{k}{number of nearest neighbors to search per query point.}

\item{radius}{numeric distance cutoff; neighbors farther than this are
dropped. Use `Inf` (default) to keep all `k` neighbors regardless of
distance.}

\item{include.self}{a boolean specifying whether to include self from the knn.}
}
\value{
sparse dgCMatrix, dim = nrow(query_coords) x nrow(ref_coords)
}
\description{
Build a sparse neighbor-weight matrix via k-NN, optionally filtered by radius
}
