% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ComputeNormalizedMoranI.R
\name{ComputeNormalizedMoranI}
\alias{ComputeNormalizedMoranI}
\title{Compute Normalized Moran's I for Spatial Ecotypes}
\usage{
ComputeNormalizedMoranI(
  scmeta,
  coords = c("X", "Y"),
  SE = "SE",
  CellType = "CellType",
  nperm = 1000,
  k = 3,
  ncores = 1
)
}
\arguments{
\item{scmeta}{A data.frame containing single-cell metadata. Each row represents a cell.}

\item{coords}{Character vector specifying column names for spatial
coordinates (e.g., \code{c("X", "Y")}).}

\item{SE}{Character. Column name specifying spatial ecotype (or cell state) labels.}

\item{CellType}{Character. Column name specifying cell type annotations.}

\item{nperm}{Integer. Number of permutations used to estimate the null distribution.
Default is 1000.}

\item{k}{Integer. Number of nearest neighbors used to construct the spatial graph.
Default is 3.}

\item{ncores}{Integer. Number of cores used in Moran’s I computation (if parallelized).
Default is 1.}
}
\value{
A numeric vector of Z-scores representing normalized Moran's I values
for each cell state.
}
\description{
Computes a normalized Moran's I statistic to quantify spatial autocorrelation
of cell states (e.g., spatial ecotypes) while accounting for cell-type structure.
The observed Moran's I is compared against a null distribution generated by
permuting cell state labels within each cell type.
}
\details{
The function performs the following steps:
\itemize{
  \item Constructs a k-nearest neighbor graph based on spatial coordinates
  \item Computes observed Moran’s I for cell state labels
  \item Generates a null distribution by permuting cell state labels within each cell type
  \item Normalizes observed Moran’s I into Z-scores using the permutation mean and standard deviation
}

This approach preserves cell-type composition while testing whether spatial
organization of cell states deviates from random expectation.
}
